package edu.wallet.server;

/**
 * Request/Response (JSON) field names.
 */
public enum Field {
    user,
    transactionId,
    balanceChange,

    errorCode,
    balanceVersion,
    outgoingBalance;
}
